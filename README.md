Functionality for storing images to Google storage or Amazon s3bucket;


Run this migration: 
 php yii migrate/up --migrationPath=@backend/images/migrations
 
run this command in terminal: sudo chmod -R 777 backend/web/img 

